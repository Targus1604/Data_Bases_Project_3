"use client"
import { Button } from "@mui/material";

export default function Home() {
  return (
    <main>
      <Button type = "button" variant = "contained">Click Me</Button>

      <Button type = "button" variant = "contained">Click Me</Button>
    </main>
  )
}
